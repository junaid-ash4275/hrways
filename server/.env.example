# Postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/hrways

# JWT
JWT_ACCESS_SECRET=change-me-access
JWT_ACCESS_TTL=15m
JWT_REFRESH_SECRET=change-me-refresh
JWT_REFRESH_TTL=7d

# Seed Admin
ADMIN_EMAIL=admin@hrways.local
ADMIN_PASSWORD=ChangeMe123

# SMTP (Password reset)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
MAIL_FROM="HRWays <no-reply@local>"

# OTP settings
OTP_TTL_MIN=10
OTP_MAX_ATTEMPTS=5
RESET_TOKEN_TTL_MIN=15

# Uploads
# Max upload size in megabytes (enforced by multer)
MAX_UPLOAD_MB=5
# Comma-separated list of allowed MIME types for document uploads
ALLOWED_MIME=application/pdf,image/jpeg,image/png
